
 Which areas of the business have the highest negative impact in sales metrics performance in 2020 for the 12 week before and after period?

region

platform

age_band

demographic

customer_type

Do you have any further recommendations for Danny’s team at Data Mart or any interesting insights based off this analysis?


## Region Sales 

```sql
WITH region_sales AS
(
SELECT region , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY region
)


SELECT region, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM region_sales

```
<img width="1657" height="401" alt="image" src="https://github.com/user-attachments/assets/8b6a16c7-5cbc-4d33-af2b-dcbd82cd58dc" />

---

# Platform Sales

```sql
WITH platform_sales AS
(
SELECT platform , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY platform
)


SELECT platform, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM platform_sales
```
<img width="1600" height="176" alt="image" src="https://github.com/user-attachments/assets/b4462393-5bde-4cf1-8794-563800b46c90" />


# Age Band Sales

```sql
WITH ageband_sales AS
(
SELECT age_band , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY age_band
)


SELECT age_band, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM ageband_sales
```
<img width="1596" height="258" alt="image" src="https://github.com/user-attachments/assets/fd547340-d6e4-40af-834b-60b489ae3e02" />

---
# Demographic Sales

```sql
WITH demographic_sales AS
(
SELECT demographic , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY demographic
)


SELECT demographic, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM demographic_sales

```
<img width="1640" height="220" alt="image" src="https://github.com/user-attachments/assets/b217e977-b385-48a2-85fc-101995bd7b13" />

---
# Customer Type Sales 

```sql
WITH customertype_sales AS
(
SELECT customer_type , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY customer_type
)


SELECT customer_type, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM customertype_sales
```

<img width="1633" height="214" alt="image" src="https://github.com/user-attachments/assets/f2f5e4ae-682f-429f-b3fc-3bebca72ddcc" />

