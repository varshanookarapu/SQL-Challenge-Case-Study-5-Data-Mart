
## Region Sales 

```sql
WITH region_sales AS
(
SELECT region , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY region
)


SELECT region, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM region_sales

```
<img width="1657" height="401" alt="image" src="https://github.com/user-attachments/assets/8b6a16c7-5cbc-4d33-af2b-dcbd82cd58dc" />

---

# Platform Sales

```sql
WITH platform_sales AS
(
SELECT platform , 
SUM(CASE WHEN week_number BETWEEN 25-12 AND 25-1 THEN sales END) AS twelve_weeks_before_sales,
SUM(CASE WHEN week_number BETWEEN 25 AND 25+12 THEN sales END) AS twelve_weeks_after_sales
  FROM cleaned_weekly_sales 
WHERE calendar_year =2020
GROUP BY platform
)


SELECT platform, 
ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL AS 
 percentage_change_in_sales_for_12_week_period,
 
RANK() OVER(ORDER BY ROUND((100*( twelve_weeks_after_sales -twelve_weeks_before_sales  ) /NULLIF(twelve_weeks_before_sales,0)),2) :: DECIMAL DESC) as rank 
 
FROM platform_sales
```
<img width="1600" height="176" alt="image" src="https://github.com/user-attachments/assets/b4462393-5bde-4cf1-8794-563800b46c90" />


# Age Band Sales

```sql
```
---
# Demographic Sales

```sql
```
---
# Customer Type Sales 

```sql
```
